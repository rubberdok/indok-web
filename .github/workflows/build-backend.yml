name: Build backend image for staging

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build backend image
        id: build-image
        uses: docker/build-push-action@v2
        with:
          context: ./backend
          file: backend/Dockerfile.prod
