FROM python:3.9
ENV PYTHONUNBUFFERED 1
RUN mkdir -p /code
WORKDIR /code
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8000

ENV DB_HOST=db

CMD [ "python", "manage.py", "runserver" , "0.0.0.0:8000", "--settings", "api.settings_prod" ]