query user {
  user {
    ...User
  }
}

query userWithEventsAndOrgs {
  user {
    ...UserWithEventsAndOrgs
  }
}

query userToEdit {
  user {
    ...UserToEdit
  }
}

query userOrganizations {
  user {
    id
    isIndok
    organizations {
      ...UserOrganization
    }
  }
}
