query User {
  user {
    ...BaseUserFields
  }
}

query EditUser {
  user {
    ...BaseUserFields
    canUpdateYear
    graduationYear
    graduationYearUpdatedAt
    allergies
    phoneNumber
  }
}
