query eventFilteredOrganizations {
  eventFilteredOrganizations {
    id
    name
    color
    children {
      id
      name
    }
  }
}

query getCategories {
  allCategories {
    id
    name
  }
}
