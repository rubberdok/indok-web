query product($productId: ID!) {
  product(productId: $productId) {
    ...Product
  }
}

query products {
  products {
    id
    name
    price
    description
    maxBuyableQuantity
  }
}

query userOrders {
  userOrders {
    ...Order
  }
}
