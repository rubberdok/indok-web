query product($productId: ID!) {
  product(productId: $productId) {
    ...Product
  }
}

query products {
  products {
    id
    name
    price
    description
    maxBuyableQuantity
  }

  allUsers {
    id
  }
}

query userOrders {
  userOrders {
    ...Order
  }
}
