fragment Product on ProductType {
  id
  name
  description
  price
  maxBuyableQuantity
  shopItem
}

fragment Order on OrderType {
  id
  quantity
  totalPrice
  paymentStatus
  timestamp
  delivered
  product {
    ...Product
  }
  user {
    id
    username
    firstName
    lastName
  }
}
