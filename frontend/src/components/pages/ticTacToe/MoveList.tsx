import { Box, ButtonBase, Typography } from "@mui/material";
import { useState } from "react";

type Props = {};

export const MoveList: React.VFC<Props> = ({}) => {
  return (
    <Typography variant="h3" align="center">
      Hei!
    </Typography>
  );
};

export default MoveList;
